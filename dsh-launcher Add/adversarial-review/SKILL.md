---
name: adversarial-review
description: >
  对抗性审查与沉淀迭代：对具体产出物（文档/代码/规范条文/方案）做证据绑定的分级对抗审查并驱动修复闭环，多轮重审只审增量以控制token消耗；生成交付物前注入前置导引单提升首轮质量；每轮自动快照，满足条件时静默或提醒沉淀（更新导引单、泛化脚本、核对模型价格）。不用于无具体对象的一般问答。触发词：「对抗性审查」「审查」「复核」「把关」「找错」「挑毛病」「审一遍」「执行沉淀」「adversarial review」。
metadata:
  sediment: true
  version: "0.1.0"
---

# adversarial-review（对抗性审查与沉淀迭代）

本技能用于：对**具体产出物**做分级对抗审查、驱动修复闭环、并在多轮运行中沉淀前置导引单与脚本资产以持续降低 token 消耗。**不适用**于：无具体对象的一般问答；用户明确说"不用审"的场景。

## 0. 启动判定（先做，不审查先判定）

1. **对象与命名空间**：判定审查对象类型 → 命名空间（regulation 规范类 / document 文档类 / code 代码类 / 不明→general 通用层并告知用户）。
2. **模式**：默认"审查+修复闭环"；用户明示"只审不修"→ 仅报告模式。用户输入"本次不审"→ 退出。
3. **生成阶段注入判定**：若当前任务是**生成**交付物且命中白名单（对外文档/规范条文/长文档/代码）或历史同类曾被送审 → 先读 `references/pregen-guide.md`，截取对应命名空间条款＋通用条款（≤600 token）注入生成上下文，并告知用户"本成果完成后将进入对抗性审查"。
4. **显式命令**：用户逐字输入"执行沉淀"→ 跳转第 7 节，不启动审查。

## 1. 参数表（默认值，均可被用户指令覆盖）

| 参数 | 默认 | 参数 | 默认 |
|---|---|---|---|
| 最大轮数 | 5 | 收敛重合度 | 0.80 |
| 导引单注入预算 | 600 token | 快照上限 | 10 KB |
| 静默迭代前提 | ≥7 条 ∧ ≥48h ∧ 峰谷 | 超期提醒前提 | ≥10 条 ∧ ≥72h |
| 单次维护预算 | 120k in / 20k out / 0.5 元 | 月度维护预算 | 暂不启用（null；填入数值即启用，建议 2 元） |
| 并行规模阈值 | 3 万字符或 10 章节 | 峰谷时区 | Asia/Shanghai |
| L1 全读上限 | 10 万字符 | 运行计数口径 | pending 快照文件数 |

## 2. 分层审查工作流

**阶段 L0（确定性检查，零模型判断）**：运行 `var/registry/scripts.json` 中适用本命名空间的脚本；脚本输出即证据。禁止用模型重算脚本已覆盖的检查项。

**阶段 L1（快筛）**：对象 ≤10 万字符（参数化）时通读全文一遍；超大对象只读目录结构与各章节首尾段做抽样快筛，禁止无差别全量读入。输出①可疑段落清单②风险热力图所需的便宜信号（引用密度、数字密度、L0 命中聚集度）。不深挖。

**阶段 L2（对抗深审）**：仅覆盖热力图高分区（score=prior×signal×impact，细则见 `references/protocols.md` 4.7，仅在需要时读取）。对抗视角默认三类：边界条件、交叉引用一致性、外部硬约束符合性。并行判据满足时才分子审查者，仲裁规则见 protocols.md 4.6。

**意见输出（必须遵守）**：每条意见必须给出 `anchor`（位置锚点）、`basis`（依据）、`confidence`；缺任一直接丢弃；禁止复制原文段落，引用一律用锚点；按严重度排序输出：阻断→严重→轻微→建议。

## 3. 修复闭环（默认模式）

1. 审查前与用户确认通过判据（默认：阻断清零∧严重清零）并冻结；循环中不得加码。
2. 用 `scripts/ledger.py` 建台账；只有阻断/严重级进入修复循环。
3. 修复仅限台账列明项，**禁止顺手修改**；修复后按台账逐条验证。
4. 第二轮起：运行 `scripts/diff_scope.py` 取增量范围，只审 diff＋关联段＋未关闭台账项；已验证部分标记冻结区不重审。
5. 收敛判定：两轮意见重合度 ≥0.80 且阻断清零 → 停止；超 5 轮 → 熔断，输出遗留清单交用户裁决。
6. 同一 issue 重开 ≥2 次 → 暂停并报告"修复规范可能不完整"，请用户裁决。

## 4. 仅报告模式

执行第 2 节后直接输出分级意见单与台账，不进入修复循环；用户后续可凭台账要求修复。

## 5. 费用控制（必须遵守）

1. 逐轮记录 token（输入/输出/命中，读不到记 null）。
2. 模型识别：识别当前模型 → 查 `references/pricing-models.md` 的 JSON 块取价格与峰谷；识别不到 → 用 default 行（λ=1/30）并明示"按默认系数估算"。
3. 压缩判定：压缩任何上下文前必须过净收益不等式（见 protocols.md 6.2）；**审查者子上下文的净化（截断/重构）不适用该不等式**——观测独立性优先。
4. 批量任务且处于高峰时段 → 给出峰谷费用差提示，由用户选择；用户即时指令优先。

## 6. 收尾（每轮完整运行必须执行）

1. 组装本轮快照（schema 见 protocols.md 7.1；含台账结论与逐轮 token 数字）→ `python3 scripts/collect_snapshot.py --ns <ns>`。
2. `python3 scripts/sediment_check.py` → 按信号行动：
   - 顶层 `signal` 字段为 `"sediment"`（按 JSON 字段解析判定；实际输出键值间有空格，勿做 `signal:"sediment"` 无空格子串匹配）：先输出告示行（"已满足沉淀条件，启动静默迭代：命名空间 X，预计 ≤Y token/约 Z 元，输入'停止沉淀'可中断"）→ 跳转第 7 节沉淀流程；
   - 顶层 `signal` 字段为 `"remind"`：回复末尾附提醒一句（24h 内已提醒过则跳过）；
   - `"none"`：无动作。

## 7. 沉淀流程

1. **触发**：静默（信号）/提醒（用户同意）/显式（"执行沉淀"）。显式命令先扫描各技能根（Kimi 用户技能目录、`~/.agents/skills`、`~/.dsh/skills`、`~/.workbuddy/skills`，按宿主实际存在的根）下所有 SKILL.md 的 frontmatter，列出含 `sediment: true` 的技能＋全部＋取消，用户确认后执行。
2. **执行**：`python3 scripts/sediment_run.py --ns <ns>` 按 S0→S6 分阶段。脚本先产出 S2 候选清单（`var/sediment/iter-N-candidates.json`）并停在该阶段等你裁决：由你撰写新条款文本并做语义裁决（降权/淘汰/合并），再以 `--guide-edits <裁决文件>` 续跑完成 S2 及后续阶段（脚本落盘）；裁决文件是 JSON：`{"add":[{"ns":"document","line":"- [D-003] …"}],"demote":["D-001"],"retire":["D-002"]}`。分阶段落盘（每阶段写 checkpoint），可断点续，可中断。
3. **可见性**：开始前一行告示（预计消耗）；思维链可见宿主在思维链声明；完成后末尾一行总结。
4. **预算**：受第 1 节参数约束；超限即截断，剩余留队。

## 8. 平台降级表

| 能力 | 完整形态 | 降级形态 |
|---|---|---|
| 迭代可见性 | 思维链＋正文即时行 | 正文即时行＋末尾总结 |
| 迭代与新审查并行 | 子代理并发 | 当前阶段落盘后串行 |
| 超期提醒 | 平台定时＋会话内检查 | 仅会话内检查 |
| 沉淀确认 | 交互选择框 | 文字单选提问 |
| 取时 | shell `date` 换算 UTC+8 | 询问宿主当前时间 |

## 9. 自检清单（收尾前逐项打勾）

- [ ] 意见单全部含 anchor/basis/confidence，无原文复述
- [ ] 台账状态机无非法转移；阻断级已清零或已呈用户裁决
- [ ] 第二轮起只审增量；冻结区未被重审
- [ ] 快照已写入 pending；sediment_check 已运行并按信号行动
- [ ] 本轮 token 已记录；预算未超限（超限已降档并明示）
- [ ] 阻断级问题已显式呈现用户

## 10. 资源索引（仅在需要时读取）

- `references/protocols.md`：协议细则（分级、状态机、仲裁、热力图、压缩不等式、快照 schema）
- `references/pregen-guide.md`：前置导引单（生成阶段注入用）
- `references/pricing-models.md`：价格系数表（含唯一 JSON 块）
- `references/namespaces/<ns>.md`：命名空间高频缺陷清单＋聚焦区先验

## 附：脚本执行与状态目录

- 五个脚本一律 Python 3.9+ 标准库、零第三方依赖，均以 `__file__` 自定位技能根，**与当前工作目录无关**，可在任意位置以绝对/相对完整路径调用；失败时输出 `{"error":…,"hint":…}` 单行 JSON 并退出非 0 码，必须如实转告用户，禁止静默跳过。
- 状态目录三级回落（解析顺序）：环境变量 `ADV_REVIEW_HOME`（用户显式指定优先）→ `<技能根>/var/`（可写时）→ `~/.adversarial-review`（保底）。五个脚本均支持 `--state-dir` 覆盖状态目录（主要供测试/演示隔离）。
